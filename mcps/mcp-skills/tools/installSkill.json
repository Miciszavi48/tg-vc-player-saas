{
  "name": "installSkill",
  "description": "Instala un skill desde skills.sh o GitHub usando su ID (owner/repo@skill)",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "skillId": {
        "type": "string",
        "description": "ID del skill a instalar, ej: owner/repo@skill"
      }
    },
    "required": [
      "skillId"
    ]
  }
}